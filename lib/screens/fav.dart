import 'package:flutter/material.dart';

class FaviourateScreen extends StatelessWidget {
  const FaviourateScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
